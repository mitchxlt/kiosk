[Unit]
Description=Kiosk Autostart

[Service]
ExecStartPre=bash -c 'if [[ ! -e "${XDG_RUNTIME_DIR}/kiosk" ]]; then sleep 120; touch "${XDG_RUNTIME_DIR}/kiosk"; fi'

[Install]
WantedBy=xdg-desktop-autostart.target
